import { createApp } from 'vue'
import App from './App.vue'
import router from './router'  // Assurez-vous que votre fichier router est importé ici

createApp(App).use(router).mount('#app')  // Utilisez le routeur ici
